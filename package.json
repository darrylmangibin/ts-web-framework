{
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "typescript": "^4.2.3"
  },
  "dependencies": {
    "axios": "^0.21.1"
  },

  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.hml"
  }
}
